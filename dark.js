// Checking for page title
if (document.title.indexOf("Facebook" != -1)) {
    // Create button
    //var btn = $("<button>");
    //btn.text("click meeeee you fraud");
    // Append to DOM
    //$("body").append(btn);
}
